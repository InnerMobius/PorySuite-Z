# ── Game repos (not part of the app) ──
/pokefirered/

# ── Porymap build artefacts (installer clones/builds these locally) ──
/porymap_src/
/porymap/
/qt_sdk/

# ── Python runtime ──
__pycache__/
*.pyc
*.pyo
.venv/
venv/
cleanenv/
_venv_off/

# ── IDE / editor ──
.idea/
.vscode/
*.swp
*~
*.autosave
Thumbs.db
.DS_Store

# ── Logs (auto-cleaned on startup) ──
crashlogs/
launch.log
*.log

# ── Cache (working files for open projects) ──
cache/

# ── User-specific runtime data ──
projects.json
settings.ini
data/projects.json
data/settings.ini
data/toolchain/
data/git_remotes.json

# ── Test artefacts ──
.pytest_cache/
pytestresults.json
