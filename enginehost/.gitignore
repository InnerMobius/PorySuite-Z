# Build artifacts + auto-generated stubs (regenerated by the build pipeline).
build/
build32/
buildwasm/
*.o
*.exe
*.wasm
stub_gfx_data.c
_main.c

# ...but DO ship the prebuilt engine the app loads at runtime.
!dist/
!dist/anim_engine.wasm
!dist/names.json
