# PorySuite-Z — Claude Context for New Threads

## What is this project?
PorySuite-Z is a unified editor for pokefirered decomp ROM hacking projects. It combines two previously separate tools:
- **PorySuite** — edits Pokemon data (species, items, moves, trainers, starters, pokedex, config)
- **EVENTide** — edits maps, scripts, events, layouts, region maps, and text

## Current state (2026-04-07)
- **Phases 1–6 complete**: Unified window, shared data, cross-editor features, VS Seeker rematch system, all data editors, settings (including Project Display Name in General page)
- **Phase 5E complete**: All EVENTide improvements done — Move Camera, Tooltips, Live Settings Reload, Hidden Item Editor
- **Phase 7 complete**: Porymap Integration — install, launch, bidirectional map sync, auto-sync on map switch, Ctrl+E feedback + window raise, full event lifecycle callbacks (create/delete/move), stock Porymap fallback via `.psinstalled` marker, Go To button in command dialogs
- **Palette Importer (2026-04-05) — COMPLETE**: "Import Palette from PNG" group box on the Graphics tab (right column, between Shiny swatches and Icon Palette). Normal/Shiny radio selector, file picker, extracts up to 16 colors from an indexed PNG, GBA-clamps to 15-bit, loads into the selected palette slot. Swatches and battle preview refresh immediately. Marked dirty → written to `.pal` on File → Save via `flush_to_disk()`. Validates `Format_Indexed8` with clear error. Lives in `ui/graphics_tab_widget.py`.
- **Overworld Graphics Editor (2026-04-06) — COMPLETE**: Full sprite-first overworld editor in the main toolbar. LEFT PANEL: category filter + search bar, scrollable thumbnail grid of all sprites (152 mapped), "+ Add New Sprite…" button, Dynamic OW Palettes (DOWP) status/enable button. RIGHT PANEL: sprite sheet view + animation-type-aware preview (surf two-row: static poses + run cycle; fishing: 4-directional rod; VS Seeker: raise sequence; walk: standard 4-dir cycle; inanimate: static; destroy: frame sequence), palette editor with "Assign to" dropdown for palette reassignment, Import from PNG, "Show in Folder" (opens Explorer with file selected). **Add New Sprite**: dialog auto-detects frame size/name/palette from PNG, writes all 6 C headers automatically, pushes new `OBJ_EVENT_GFX_` constant to EVENTide's ConstantsManager immediately (no save/refresh needed). **DOWP patch**: one-way button patches 5 C source files to enable per-sprite palettes. **Cross-tab sync**: `gfx_constants_changed` signal → `_refresh_eventide_constants()` → event editor gfx dropdown repopulates. Files: `ui/overworld_graphics_tab.py`, `core/overworld_sprite_creator.py`, `core/dynamic_ow_pal_patch.py`.
- **Trainer Graphics Tab (2026-04-06) — COMPLETE**: New "Graphics" sub-tab alongside "Trainers" and "Trainer Classes" in the trainers section. Dropdown to select any TRAINER_PIC_* constant (friendly names shown), 128×160 sprite preview rendered with the current palette via `_reskin_indexed_png`, editable 16-colour palette swatch row (reuses `PaletteSwatchRow`), "Import Palette from PNG" (extracts colour table from indexed PNG, GBA-clamps to 15-bit), file picker defaults to `graphics/trainers/palettes/`, "Open Palettes Folder" button, immediate preview refresh on palette changes, saves JASC-PAL `.pal` files via `flush_to_disk()` on File → Save. Combo box wheel-scroll disabled per project rules. New file: `ui/trainer_graphics_tab.py`. Main window wiring in `ui/mainwindow.py`.
- **Sound Editor Phases 1-3 (2026-04-06) — COMPLETE**: Full GBA M4A audio engine in Python. Parses all 347 songs, 77 voicegroups, 89 instrument samples. Songs Tab in toolbar with browse/filter/play. Accuracy fixes: GBA-accurate MidiKeyToFreq pitch, CGB ADSR conversion, BEND subtracts C_V, running status parsing, proper volume pipeline (no double velocity/master_volume), GBA linear panning with per-note application, XCMD continuation line parsing (was creating phantom KEYSH in ~85 songs), keysplit_all voicegroup overflow for drum tracks. TONEDATA_TYPE_FIX investigated: only affects DirectSound (already handled), CGB _alt instruments pitch-shift normally. Debug log at `docs/SOUND_ENGINE_DEBUG_LOG.md`. All 18 tests pass.
- **Sound Editor Phase 4 (2026-04-06) — COMPLETE**: Instruments Tab added as a sub-tab alongside Songs in the Sound Editor. Shows 144 unique instruments (deduplicated, filler removed) grouped by type: Samples (89), Square Waves (32), Prog. Waves (11), Noise (2), Keysplits (10). Detail panel has editable controls: ADSR sliders with live visual curve, base key spinner, pan spinner, duty cycle dropdown (square), sweep spinner (square_1), period dropdown (noise). Edits propagate to every copy across all voicegroups automatically. 3-octave clickable piano keyboard with octave shift buttons (◀/▶) and range label. Step 4.4 Sample Management: export to WAV, import from WAV (pure Python conversion, no wav2agb dependency) with size/rate preview and downsample options for high-rate WAVs, replace sample audio (auto-resamples to original rate, preserves loop flag), delete unused samples with voicegroup reference checking. Step 4.5: Right-click "Go to Instrument" on Songs tab tracks jumps to Instruments tab with that instrument selected. Comprehensive tooltips on all controls. Files: `ui/instruments_tab.py`, `ui/sound_editor_tab.py`, `core/sound/sample_loader.py`.
- **Sound Editor Phase 5 (2026-04-06) — COMPLETE**: Voicegroups Tab added as a third sub-tab in the Sound Editor. Browses all 77 voicegroups with slot counts and song usage. Full 128-slot editor: change instrument type (all 13 voice types), assign samples/waveforms via picker dropdowns, edit base key/pan/duty/sweep/period/ADSR. Management: add new VG (128 filler slots), clone VG (deep copy), delete VG (reference-checked). Right-click: copy instrument from another VG, go to instrument. Cross-reference shows songs using each VG. Saves to `voice_groups.inc` with correct assembly syntax. Also fixed: sample replace now resamples to original rate (preserves pitch), "No Map Loaded" save error fixed, QSlider scroll guards added. File: `ui/voicegroups_tab.py`.
- **Sound Editor Phase 6 (2026-04-06) — COMPLETE**: Integration with PorySuite-Z. EVENTide's playbgm/playse/playfanfare command widgets have ▶ Preview, ■ Stop, and 🔊 Open in Sound Editor buttons. Preview plays in background WITHOUT switching tabs (stays on Event Editor). F8 keyboard shortcut jumps to Sound Editor. Save pipeline covers voice_groups.inc, song_table.inc, songs.h, midi.cfg. Sound settings page in Settings dialog (preview volume, loop count, auto-downsample rate, stereo/mono output mode). Songs loop during preview (default 2 iterations, configurable). Constants sync: switching from Sound Editor to EVENTide triggers ConstantsManager.refresh() so new/removed songs appear in dropdowns. Porymap reads songs.h directly and watches for changes — no bridge code needed.
- **Sound Editor Phase 7 (2026-04-06) — COMPLETE**: MIDI Import wizard — full 5-page flow. Page 0: pick .mid file (shows tracks/tempo/duration). Page 1: voicegroup + conversion settings. Page 2: per-track instrument mapping (GM instrument → VG slot with live name display, Auto-Match by Name, post-processes VOICE commands after mid2agb). Page 3: song structure — full section sequencer where users define named sections (start/end measures), arrange them in a custom play order (drag/reorder), and set which position starts the loop (intro plays once, loop point onward repeats); quick presets (Automatic, Loop All, No Loop) for simple cases; post-processor translates structure into GOTO/PATT/PEND assembly. Page 4: runs mid2agb, registers in song_table.inc + songs.h + midi.cfg. `MidiFileInfo` includes `total_measures`, `time_sig_num`, `time_sig_den` from the MIDI. New dependency: `mido>=1.3`.
- **Sound Editor Phase 8 — COMPLETE (Steps 8.1-8.6 ALL DONE)**: Piano Roll Composer. Step 8.1 — Read-Only Piano Roll View: `ui/piano_roll_widget.py` (QPainter canvas with mini keyboard, measure ruler, beat/sub-beat grid, colored note bars with velocity opacity, loop region highlight, playback cursor, Ctrl+Wheel zoom, hover note name) and `ui/piano_roll_window.py` (standalone QMainWindow with track tab bar, zoom sliders, grid snap combo with scroll guard, song info label, keyboard shortcuts, Play/Stop/Pause transport with Space bar toggle, moving cursor, volume control, time display). "Piano Roll" button added to Songs tab transport controls in `ui/sound_editor_tab.py`. Step 8.2 — Piano Roll Editing (2026-04-07): click to place notes (grid-snapped), drag-right to extend duration, click-drag note body to move (time + pitch), drag right edge to resize, right-click to delete, Shift+drag box selection, Ctrl+click toggle selection, Delete/Backspace delete selected, Ctrl+A/Escape/Ctrl+C/Ctrl+V, yellow highlight on selected notes, velocity thin bar at bottom of each note, snap grid dropdown (1/4, 1/8, 1/16, 1/32, Free), context-sensitive cursors (crosshair/open hand/resize arrow). Step 8.3 — Track Management (2026-04-07): `ui/piano_roll_tracks.py` — 220px sidebar with per-track rows (color swatch, name, instrument number + name, channel, note count), volume slider (0-127), pan slider (0-127, 64=center), Mute/Solo toggles with colored highlights, click-to-select track, Add/Remove/Duplicate track buttons. Step 8.4 — Real-Time Sequencer (2026-04-07): Piano roll uses a true real-time sequencer — notes are synthesized on-the-fly as the cursor crosses them on the timeline (no pre-rendering). `core/sound/realtime_sequencer.py` — sounddevice callback advances tick clock per-sample, mixes active voices with numpy. Note rendering happens on a background worker thread (not in the callback) to prevent buffer underruns and tempo wobble. 32-voice polyphony, 4-second max note render, per-track mute/solo/volume/pan. Bug fixes (2026-04-07): voicegroup/instrument swaps update live sequencer in-place, track volume/pan sliders wired, pause/resume handles missing sequencer, proper cleanup on destroy. Performance fix: vectorized looping sample resample in `render_directsound` (was a Python for-loop). Song structure fix: `load_song_data` now flattens PATT/PEND/GOTO via `flatten_track_commands` so songs like Game Corner show their full intro-then-loop structure. Ruler scrub: click-and-drag on ruler to set playback position (red triangle marker visible). Files: `core/sound/realtime_sequencer.py` (NEW), `core/sound/audio_engine.py` (vectorized), `ui/piano_roll_window.py` (rewritten), `ui/piano_roll_widget.py` (ruler_clicked + flatten + scrub). Step 8.5 — GM Voicegroup Generator (2026-04-06): `core/sound/gm_voicegroup.py` scans all 77 voicegroups, catalogs 89 unique DirectSound samples, maps them to GM program numbers (0-127) by SC-88 Pro/SD-90 name matching, creates a 128-slot voicegroup with real instruments for matched slots and square wave fallback for unmatched. Coverage report function. "Generate GM" button added to Voicegroups tab toolbar and MIDI Import dialog voicegroup selection page. MIDI import auto-detects existing GM voicegroup and pre-selects it. Step 8.6 — Round-Trip Editing (2026-04-07) COMPLETE: `core/sound/song_writer.py` (NEW) converts SongData back to .s assembly format with full round-trip fidelity. Save follows PorySuite standard pattern (edits in RAM, modified signal propagates up, File → Save writes disk). `notes_to_track_commands()` converts visual notes to track commands. Preserves PATT/GOTO structural commands for songs that have them. UI overhaul (2026-04-07): RulerWidget extracted as separate fixed widget above scroll area (never scrolls away), toolbar compacted (track tabs → dropdown, song info → status bar). All Phase 8 steps complete.
- **Abilities Editor (Phase 8A, 2026-04-07) — COMPLETE**: Full editor for ability data in the PorySuite toolbar. Searchable browser with detail panel: display name (12-char limit with counter/color), constant name (read-only, renamed via Rename button + RefactorService), description (52-char limit with overflow highlighting in both detail panel and Add dialog), species usage table with double-click cross-navigation to Pokemon tab. **Visual Battle & Field Effect Editor**: dropdown-driven editors that detect the current ability's effect by parsing C source, let users pick a category (Status Immunity, Contact Status, Type Absorb, Weather, Stat Boost, Intimidate, Contact Recoil, Pinch Type Boost, Type Immunity, Weather Recovery, Type Trap, Crit Prevention for battle; Encounter Rate Halve/Double, Type Encounter Rate, Post-Battle Pickup, Guaranteed Escape, Faster Egg Hatching, Nature Sync, Gender Attract for field) and configure parameters (which status/type/stat/weather, chance%, HP fraction). Shows live C code preview. Writes real C code to the correct files (battle_util.c, wild_encounter.c, pokemon.c, battle_script_commands.c, battle_main.c) on save, with automatic removal of old code when switching templates. Add New Ability dialog: auto-derives constant from display name, separate Battle Effect and Field Effect copy-from dropdowns. Delete with safety scan. Rename uses full RefactorService pattern. Files: `ui/abilities_tab_widget.py`, `core/ability_effect_templates.py`, `core/refactor_service.py`, `ui/custom_widgets/rename_dialog.py`, `ui/mainwindow.py`.
- **Piano Roll UX Polish (2026-04-07) — COMPLETE**: Song Structure Panel on right side shows LABEL/GOTO/PATT/PEND/FINE as friendly names (Section, Loop Back, Play Pattern, End Pattern, End Song) — add, remove, edit, click to seek. Save button on toolbar + Ctrl+S. Instrument selector changed from number spinner to full dropdown with instrument names. Voicegroup friendly labels system (`core/sound/voicegroup_labels.py`) — "Auto-Label" button in Voicegroups tab generates names from song usage, pencil button for individual rename, stored in PorySuite cache (never touches source). VG dropdown refreshes live (picks up newly created GM voicegroups). Files: `ui/piano_roll_structure.py` (NEW), `core/sound/voicegroup_labels.py` (NEW), `ui/piano_roll_window.py`, `ui/piano_roll_tracks.py`, `ui/voicegroups_tab.py`, `ui/sound_editor_tab.py`.
- **Sound Editor Phase 9 (2026-04-07) — COMPLETE**: Import song .s file from another project. "Import .s..." button on Songs tab opens a 3-page wizard: pick .s file with full song preview (tracks, tempo, voicegroup, loop info), voicegroup compatibility check with remap option, automatic copy + label rewrite + registration. Import dialog uses display name input (not raw constant) with MUS_/SE_ type selector and auto-derived constant preview. Steps 9.1 (export) and 9.2 (QoL) marked NOT NEEDED. Files: `ui/dialogs/s_file_import_dialog.py` (NEW), `ui/sound_editor_tab.py`.
- **Song Management (2026-04-07) — COMPLETE**: Right-click context menu on Songs list with Rename, Replace, Export, Delete. "Rename Song..." asks for display name, derives constant, updates all config files + renames .s file. "Replace with .s File..." overwrites a song's music data with another .s file while keeping the same constant and registration (labels are rewritten automatically). "Export .s File..." saves a copy of the song's assembly file. "Delete Song" checks cross-references, warns, removes from all config files + deletes .s file + re-indexes. Files: `core/sound/song_table_manager.py`, `ui/sound_editor_tab.py`.
- **Piano Roll Bug Fixes (2026-04-07) — COMPLETE**: Save/reload corruption fixed (loop_count=1→0 prevents note duplication through save cycles). Multi-select drag fixed (all selected notes move together, not just the grabbed one). Ctrl+A respects track filter (single-track view selects only that track's notes). Voicegroup rename button + double-click in Voicegroups tab. Files: `ui/piano_roll_widget.py`, `ui/voicegroups_tab.py`.
- **GM Voicegroup Deduplication (2026-04-07) — COMPLETE**: Generator no longer floods 128 slots with identical square wave fallbacks. Empty slots get silent filler. Voicegroup is trimmed to end at the last real instrument (no trailing empty slots). All unique non-DirectSound instruments (square waves, programmable waves, noise) from existing voicegroups are cataloged and placed in appropriate GM slots (synth leads, pads, percussion). File: `core/sound/gm_voicegroup.py`.
- **Save & Git Confirmation Dialogs (2026-04-07) — COMPLETE**: File → Save / Ctrl+S asks "Save all changes to disk?" before writing. Piano roll save button warns that it writes the .s file directly (not deferred to File → Save). Git Push/Pull confirmations are non-suppressible with explicit warnings about data loss/overwrite. Files: `ui/unified_mainwindow.py`, `ui/piano_roll_window.py`, `ui/mainwindow.py`, `core/suppress_dialog.py`.
- **Backlog (roadmap)**: Phase 9 Pokedex Habitat/Area Display (see `docs/UNIFIED_EDITOR_PLAN.md`)
- **Name Decapitalizer (2026-04-05) — COMPLETE (all bugs fixed)**: `Edit → Name Decapitalizer…` opens a preview-and-apply dialog that batch-converted ALL-CAPS display names to Smart Title Case across 7 categories: species, moves, items, trainers, trainer classes, abilities, UI key strings. Dialogue scripts untouched. Only converted strings whose letters were ≥70% uppercase (preserved hand-customized names). Filler words (of/the/and/in/on/to/at/for/by/or/as/vs/de/la/du/nor/but) stayed lowercase mid-string. Roman numerals (II–XV, XX) kept upper. User-editable skip-list (seeded with HM, TM, PP, HP, EXP, PC, LV, STR, DEF, ATK, SPE/SPA/SPD/SP, OK, OT, ID, VS, AI, IV, EV, KO, CPU, NPC, TV, PS) persists in `settings.ini` under `[NameDecapitalizer]/skip_list`. `TM42`/`HM03` style kept upper. Apostrophes/hyphens preserved (`FARFETCH'D → Farfetch'd`). Species/moves/items/trainers/trainer-classes/UI go through existing in-memory setters and are saved on File → Save; ability names are written directly to `src/data/text/abilities.h` on apply; species names are written directly to `src/data/text/species_names.h` on apply via `_write_species_names_header()` (final bug fix — previously only the Rename tool wrote this file, so decapitalized species names never persisted to the ROM). Items list + trainer-class list widgets refresh live after apply. Module: `ui/name_decapitalizer.py`.
- **Graphics Tab Refactor (2026-04-05) — COMPLETE**: Full 3-column replacement of the Pokemon Graphics sub-tab. LEFT: front/back/footprint thumbnails + Open Folder. CENTER: Battle scene preview (`res/images/BattleBG.png` + sprites + `Shadow.png` + `BattleTextBox.png`) + Player Y/Enemy Y/Enemy Altitude spinboxes + Show Shadow + Preview Shiny checkboxes. RIGHT: Normal palette (16 swatches), Shiny palette (16 swatches), Menu Icon Palette with 64×64 live animated icon preview + 0/1/2 dropdown + 3 editable shared icon palette rows. Writes to `src/data/pokemon_graphics/{front,back}_pic_coordinates.h`, `enemy_mon_elevation.h`, `src/pokemon_icon.c` (gMonIconPaletteIndices), `graphics/pokemon/<slug>/{normal,shiny}.pal`, and `graphics/pokemon/icon_palettes/icon_palette_{0,1,2}.pal`. All edits mark the window dirty; `flush_to_disk()` runs at save_data() start. 15-bit GBA color clamping on every palette write. Shadow uses pokefirered's fixed `(enemy_x, enemy_y + 29)` position and is hidden when elevation==0 (matching `SetBattlerShadowSpriteCallback`). Shiny-preview and icon-preview reskins preserve original PNG alpha so transparency works. Colour picker = Qt's full visual picker with the `HTML:` label auto-renamed to `Hex:`, reparented to the top-level window and painted via `QPalette` on swatches so stylesheets don't cascade into the dialog. New modules: `ui/palette_utils.py`, `ui/graphics_data.py`, `ui/graphics_tab_widget.py`.
- **Sound Test — Pokemon Cry Preview (2026-04-05) — COMPLETE (cries only)**: `ui/audio_player.py` shared `AudioPlayer` (QMediaPlayer). **▶ Play Cry** button on Pokemon tab + Pokedex tab, **▶ Preview** button on Event Editor `playmoncry` command. Species constant → lowercase slug → `sound/direct_sound_samples/cries/<slug>.wav`. Missing-file warning with expected path. SE_*/MUS_*/fanfare preview deliberately deferred (requires GBA audio emulation).

## Key architecture
- `unified_mainwindow.py` — The main window. Creates hidden PorySuite + EVENTide windows, pulls their widgets into a QStackedWidget with RPG Maker XP-style icon toolbar
- `mainwindow.py` — The original PorySuite window (8600+ lines). Still has all data logic
- `eventide/mainwindow.py` — The original EVENTide window. Still has all map/script logic
- `app.py` — Launcher. `_launch_unified()` creates both windows, loads data, creates UnifiedMainWindow
- `projectselector.py` — Project picker. Single "Open" button per project

## Data layer
- PorySuite: `source_data` (PokemonDataManager via plugin system, JSON cache of C headers)
- EVENTide: `ConstantsManager` (class-level attrs parsed from C headers) + `eventide_utils` (text.inc/scripts.inc parsing)
- Unified save triggers both save paths; dirty tracking shows `[*]` in title bar

## Editor tabs (PorySuite side)
| Tab | Sub-tabs / Features |
|-----|-------------------|
| Pokemon | Info, Stats, Images — full species editor with evolution chains |
| Pokedex | National/Regional dex editors with detail panel |
| Items | Searchable list, detail editor, **editable icon picker** (writes item_icon_table.h), "Open Icon in Folder" |
| Moves | Searchable/filterable list, detail editor with effect descriptions |
| Trainers | **Trainers** sub-tab (party editor, rematch tiers) + **Trainer Classes** sub-tab (name/money/sprite editing, add new classes, battle info, facility classes) + **Graphics** sub-tab (trainer sprite preview, palette editing, import palette from PNG) |
| Overworld GFX | Sprite-first browser (152 sprites), category filter + search, animation-type-aware preview (walk/surf/fish/VS Seeker/inanimate/destroy), palette editor with reassignment, Add New Sprite, Dynamic OW Palettes patch, cross-tab sync to EVENTide |
| Starters | Three starter slots with species, level, item, moves |
| UI | Name Pools, Location Names, Key Strings |
| Abilities | Searchable browser, detail editor (name/constant/description), visual battle effect editor (13 templates), visual field effect editor (8 templates), species usage with cross-nav, add/duplicate/rename/delete |
| Config | config.mk and config.h editors |

## Editor tabs (EVENTide side)
| Tab | Features |
|-----|---------|
| Event Editor | RMXP-style event editing with pages, conditions box, commands, color coding, sprite preview, position overrides, cross-references, script lookup |
| Maps | Map renaming, group management, warp validation |
| Layouts | Layout rename/delete, tileset assignment |
| Region Map | Visual editor for Kanto + Sevii regions |

## Phase 5E — EVENTide Improvements — COMPLETE

### Event Editor features
- **RMXP Conditions Box**: Editable flag/var conditions above Event Properties (flag picker + is ON/OFF, var picker + operator + value)
- **Numbered Page Tabs**: 1, 2, 3 instead of script label names
- **Set Move Route Rework**: Auto-scaffold movement labels, RMXP-style Move Route Editor popup with 6 category tabs
- **Color Scheme**: RMXP-style coloring — flow (red), conditionals (amber), movement (maroon), flags (purple), items (lime), sound (orange), screen (teal), battles (red), pokemon (gold). Customisable in Settings → Event Colors.
- **Position Overrides**: X/Y spinboxes update per condition page based on OnTransition scripts' setobjectxyperm commands
- **Cross-References**: Clickable notes when other scripts modify an NPC's position, with X/Y coordinates shown inline
- **Set Flag → Page Linking**: setflag/setvar commands show "→ activates Page N" and Go To jumps there
- **Script Lookup** (Ctrl+Shift+F): Project-wide search across 5,300+ labels, navigates to map/event/page
- **External Script Resolution**: Recursive loading of shared scripts from data/scripts/
- **Display Overhaul**: Human-readable names for all constants (trainers, items, species, moves, flags, vars)
- **Script Templates**: New Script menu with NPC, Sign, Map Script, Standard Wrapper, and Field Object templates
- **Constant Label Manager**: Standalone toolbar page for flag/var labels (porysuite_labels.json)
- **Move Camera Command**: Cutscene camera tool with 6 tabs (Pan, Slide, Screen, Effects, Timing, Sound). Auto-generates applymovement LOCALID_CAMERA blocks wrapped in SpawnCameraObject/RemoveCameraObject.
- **Comprehensive Tooltips**: Descriptive hover tooltips on all main UI controls, all command edit dialogs, all camera dialog buttons, and all ~80 commands in the command selector palette. Toggleable via Settings → Event Editor Tooltips.
- **Live Settings Reload**: All Event Editor settings (colors, tooltips) apply immediately when the Settings dialog is closed — no restart needed. `reload_settings()` on EventEditorTab handles reloading and recoloring.
- **Hidden Item Editor**: Selecting a hidden_item bg_event swaps the command list for a dedicated property panel (item picker, flag picker, quantity, position, elevation, underfoot). New Hidden Item creation via New Script menu. Delete button. Combo label updates live.

### Other Phase 5 features
- Trainer Class Editor, Open in Folder buttons, editable item icons (all complete)

## Phase 7 — Porymap Integration (working, polish remaining)

PorySuite-Z integrates Porymap as a companion map/tile editor. The two apps communicate via a JSON bridge file, command polling, and shared file watchers, behaving like one tool.

**Architecture:**
- `porymap_bridge/` — Python modules for bridge communication, launching, installing
- `porymap_patches/apply_patches.py` — Python patcher adds 11 callbacks + 5 query functions + `openMap` + `readCommandFile` + CLI arg handling to Porymap's C++ source
- `porysuite_bridge.mjs` — JS companion script runs inside Porymap, handles all callbacks, writes bridge messages, and polls for PorySuite commands every 500ms
- `SharedFileWatcher` — monitors `map.json`, `scripts.inc`, `layouts.json`, `map_groups.json` for external changes

**What's working:**
- Install pipeline (Tools > Install Porymap) with compile progress streaming
- "Open in Porymap" opens to the correct map (CLI args + QDir::cleanPath fix)
- If Porymap is already running, PorySuite sends a command file and Porymap switches maps (bidirectional sync via readCommandFile + bridge polling)
- Duplicate window prevention (Win32 window enumeration + bring-to-front)
- Bridge watcher (18 signals), launcher with config writing, bridge script auto-injection
- C++ patcher survives `git reset --hard` (Install Porymap re-applies all patches)

**All done.** Ctrl+E handler raises the unified window and logs selection feedback. Event create/delete/move callbacks all wired. Stock Porymap fallback via `.psinstalled` marker — launcher strips patched-only CLI args and skips command-file writes when missing.

### Event Editor enhancements (2026-04-05)
- **Go To → button in command dialogs**: Any command that references a script label (call, goto, call_if_*, goto_if_*, etc.) now shows a Go To button in its edit popup. Saves edits and navigates to the target script.
- **Plain English display names**: setworldmapflag, .byte 0, map script types, fadescreen, setweather, sound commands, turnobject, showobjectat/hideobjectat all show friendly names instead of raw ALL_CAPS constants.
- **Auto-sync**: Switching maps in PorySuite automatically tells Porymap to follow (no manual click needed). Anti-echo dedup prevents ping-pong loops.
- **Command palette refactor**: The 3-page Add Command palette was reorganized everyday-use-first — page 1 now has lock/faceplayer/applymovement/trainerbattle/message/giveitem/setflag/wildbattle at the top. Added 19 missing commands (trainerbattle_rematch variants, healplayerteam, getplayerxy, random, setobjectxyperm, savebgm, fadedefaultbgm, buffernumberstring, bufferstring, all 6 call_if_* comparison operators).

### Cross-Editor Live Bridge (2026-04-05)
- **Live script state sync**: `EventEditorTab._sync_live_script_state()` runs on every `_mark_dirty()` call, committing `_cmd_tuples` → active page dict AND mirroring every page into `_all_scripts[label]`. The module-level `_ALL_SCRIPTS` is the same dict reference, so other tabs reading it see current in-RAM state without save.
- **Shared texts dict**: `_ALL_SCRIPTS['__texts__']` is now the same reference as `self._texts` (was previously a copy that dropped edits). `_ALL_SCRIPTS['__texts_map__']` exposes which map those texts belong to.
- **Trainers → Dialogue tab**: Reads from both disk `.inc` files AND the Event Editor's live state via `_get_live_event_editor_state()`. Live entries are tagged `(live — unsaved edits)` in the group box title. New trainers get Settings-driven default dialogue seeded into a `(Pending)` group box that self-clears once the trainer is placed on a real map.

## Stale-State Guards (2026-04-05)
- **Trainer Class Editor preserves dirty edits across tab switches**: `_load_trainers_editor` (mainwindow.py) skips re-calling `trainer_class_editor.load()` when `has_edits()` is True. Previously every tab re-entry wiped pending name/money/sprite edits via unconditional `.clear()` calls.
- **Watcher-safe reload**: `EventEditorTab.reload_current_map(force=False)` checks `isWindowModified()` and shows a Save/Discard/Cancel dialog before clobbering unsaved edits. All watcher-driven callers (Porymap bridge, SharedFileWatcher) use the guarded default; pass `force=True` only for intentional reloads.
- **ConstantsManager refresh on tab switch**: `ConstantsManager.refresh()` re-reads all header files from the cached `_root`. Wired into `_on_stack_page_changed` so switching from a PorySuite page to an EVENTide page (events/maps/layouts/regionmap) refreshes item/flag/var/trainer constants automatically.

## Standardised text editing
- `ui/game_text_edit.py` — `GameTextEdit` widget for all game text editing
- `ui/dex_description_edit.py` — `DexDescriptionEdit` for item/dex descriptions
- 36 chars/line for dialogue, per-line colour-coded counter, overflow highlighting
- {COMMANDS} highlighted in blue, don't count toward limit

## Save pipeline (simplified)
1. `save_species_data()` — capture current species UI → memory
2. `save_items_table()` + `save_icon_changes()` — items + icon table
3. `_save_trainer_classes()` — trainer class names, money, sprite mappings
4. `save_data()` — write JSON caches
5. Direct header writers — species_info.h, pokedex_entries.h, items.h, learnsets, etc.
6. `parse_to_c_code()` — trainers.h, battle_moves.h (old pipeline, skipped for files handled by direct writers)

## File locations
- Project root: `C:\GBA\porysuite`
- Test copy: `C:\GBA\porysuite\pokefirered` (never touch `C:\GBA\pokefirered`)
- Read-only reference: `C:\GBA\READONLYREFERENCE\pokefirered`
- Settings INI: `C:\GBA\porysuite\data\settings.ini`
- Toolbar icons: `C:\GBA\porysuite\res\icons\toolbar\`

## Important rules
- Always explain in plain English, no jargon
- Stop and ask when uncertain
- Test instructions must be precise and step-by-step
- Never touch `C:\GBA\pokefirered` during porysuite work
- Log all changes in CHANGELOG.md
- Document changes in related .md files
- QComboBox must never scroll on wheel when closed (Chrome Remote Desktop safety)
- No hardcoded game-specific labels — user's hack is Zelda/Hyrule themed
